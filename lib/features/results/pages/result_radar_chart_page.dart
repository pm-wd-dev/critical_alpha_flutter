import 'package:flutter/material.dart';
import '../../../core/constants/app_constants.dart';

class ResultRadarChartPage extends StatelessWidget {
  const ResultRadarChartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: AppColors.background,
      appBar: AppBar(title: const Text('Radar Chart Analysis')),
      body: const Center(
        child: Text('Radar Chart Page - Coming Soon'),
      ),
    );
  }
}